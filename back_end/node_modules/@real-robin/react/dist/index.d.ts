import React from 'react';

declare function Robin({ apiKey, robinURL, extraHeaders, }: {
    apiKey: string;
    robinURL?: string;
    extraHeaders?: Record<string, string>;
}): React.ReactElement;

export { Robin };
